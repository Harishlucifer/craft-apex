// English menu labels equal the backend `name` field — handled by defaultValue
// in the t() call. Keep this map empty; presence forces the namespace to exist
// in i18next so lookups don't warn.
export const menu = {} as const;
