/**
 * Moved to `@craft-apex/shared` so the partner and consumer portals can use
 * the same grid chrome. Re-exported here because ~91 list pages import it
 * from this path.
 */
export {
  DataTableShell,
  TABLE_HEADER_ROW_CLASS,
  TABLE_HEAD_CLASS,
  TABLE_ROW_CLASS,
  type DataTablePagination,
  type DataTableShellProps,
} from "@craft-apex/shared";
