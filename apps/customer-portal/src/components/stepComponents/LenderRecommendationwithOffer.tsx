import { StepComponentProps } from "@/pages/WorkflowStepComponentLoader"
import { forwardRef } from "react"

const LenderRecommendationwithOffer=forwardRef((props: StepComponentProps, ref) => {
    return (
        <div>
            <h3>Lender Recommendation with Offer Selection</h3>
        </div>
    )
})

export default LenderRecommendationwithOffer;